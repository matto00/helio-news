# helio-news — daily local news aggregator → helio dashboards
#
# All helio writes go through the helio MCP server (auth lives there), so we
# talk to it with the MCP Python SDK as a stdio client — never the REST API
# directly.
feedparser>=6.0.11      # RSS/Atom ingestion
PyYAML>=6.0.2           # config/outlets.yaml
requests>=2.32.0        # ollama HTTP (no API keys)
yfinance>=0.2.40        # stock price data for the stock enricher (no API key)
trafilatura>=1.12.0     # full article-body extraction (readable text from a URL)
mcp>=1.2.0              # MCP client (spawns the helio MCP server over stdio)

# Optional — only needed when the research agent is enabled (research.enabled in
# config). Imported lazily; the pipeline runs without it when research is off.
anthropic>=0.116.0      # research enricher: Claude + web search for long-tail series
